name: Keep Backend Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # Run every 5 minutes
  workflow_dispatch:

jobs:
  ping-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          curl -m 20 -s https://disha-backend-7xmc.onrender.com/ > /dev/null
